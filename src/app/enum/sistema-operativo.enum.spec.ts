import { SistemaOperativoEnum } from './sistema-operativo.enum';

describe('SistemaOperativoEnum', () => {
  it('should create an instance', () => {
    expect(new SistemaOperativoEnum()).toBeTruthy();
  });
});
